Калькулятор, модули 26.02.2023
https://www.youtube.com/watch?v=zhnScPqcBKY
--Лучше в функцию передать аргумент, чем глобальную переменную, так будет нагляднее.
  методы получения узла (document.getElementById()) лучше вынести в начало программы, 
    а их значение(textContent) уже обновлять перед сами использованием

  Лучше избегать огромных строк таких как Number(document.getElementById().trim())
  export(var1, var2) можно из модуля экспортировать сразу несколько переменных, а не каждый.